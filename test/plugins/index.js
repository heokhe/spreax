const { spawn } = require('child_process')
const { resolve } = require('path')

module.exports = (on, config) => {
  // spawn('yarn', ['build'], {
  //   stdio: 'inherit'
  // })
  // try {
  //   spawn('node', [resolve(__dirname, '../runner.js')], {
  //     stdio: 'inherit'
  //   })
  // } catch (e) {
  //   if (e.code === 'EADDRINUSE') return true
  //   else throw e
  // }
}